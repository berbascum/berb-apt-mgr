# Another bash script to manage an apt repo

This script can be runned from any dir if it's in PATH

Change to the apt repo dir is needed first

The files \"key-ids.conf\" and \"apt-repo.conf\" need to be configured and moved to the apt repo rootdir

To create the initial dir structure, run the script with --mkdirs arg

To rebuild repo, run the script with --rebuild tag
